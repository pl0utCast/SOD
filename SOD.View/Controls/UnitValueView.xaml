<UserControl
    x:Class="SOD.View.Controls.UnitValueView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SOD.View.Controls"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignWidth="250"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <TextBox
            materialDesign:HintAssist.Hint="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:UnitValueView}}, Path=FieldName}"
            FontSize="24"
            Style="{StaticResource MaterialDesignFloatingHintTextBox}"
            Text="{Binding Value, UpdateSourceTrigger=LostFocus}" />
        <ComboBox
            Grid.Column="1"
            Margin="5,0,0,0"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            ItemsSource="{Binding UnitTypes}"
            SelectedItem="{Binding SelectedUnitInfo}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock FontSize="24" Text="{Binding Name}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</UserControl>
