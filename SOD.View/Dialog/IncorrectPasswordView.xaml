<UserControl 
    x:Class="SOD.View.Dialog.IncorrectPasswordView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:SOD.View.Dialog"
    xmlns:lc="clr-namespace:SOD.LocalizationService;assembly=SOD.LocalizationService"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="24"
                Text="{lc:Localization Login.IncorrPass}"
                Margin="30"/>
        </Grid>
        <Grid Grid.Row="1">
            <Button
                Width="150"
                Height="40"
                Content="OK"
                FontSize="24"
                Command="{Binding Incorrect}"
                Margin="15">
            </Button>
        </Grid>
    </Grid>
</UserControl>