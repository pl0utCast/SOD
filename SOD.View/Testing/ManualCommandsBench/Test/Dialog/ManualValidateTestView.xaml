<UserControl
    x:Class="SOD.View.Testing.ManualCommandsBench.Test.Dialog.ManualValidateTestView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SOD.View.Testing.ManualCommandsBench.Test.Dialog"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:lc="clr-namespace:SOD.LocalizationService;assembly=SOD.LocalizationService"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <materialDesign:Card Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Text="{lc:Localization Testing.ManualCommandsBench.CheckResult}"
                FontSize="24"/>
            <ItemsControl Grid.Row="1" ItemsSource="{Binding Posts}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="{Binding PostsColumn}" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid MinHeight="200">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto" />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Margin="0,20,0,0"
                                HorizontalAlignment="Center"
                                FontSize="24"
                                Text="{Binding Name}" />
                            <StackPanel
                                Grid.Row="1"
                                Margin="10"
                                HorizontalAlignment="Center"
                                Orientation="Horizontal">
                                <RadioButton
                                    Height="50"
                                    FontSize="24"
                                    IsChecked="{Binding Valid}"
                                    Content="{lc:Localization Testing.SODBench.Valid}"
                                    Style="{StaticResource MaterialDesignTabRadioButton}"/>
                                <RadioButton
                                    Height="50"
                                    FontSize="24"
                                    IsChecked="{Binding UnValid}"
                                    Content="{lc:Localization Testing.SODBench.UnValid}"
                                    Style="{StaticResource MaterialDesignTabRadioButton}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Button
                Grid.Row="2"
                Height="50"
                Command="{Binding Close}"
                Content="{lc:Localization Testing.ManualCommandsBench.Confirm}"
                FontSize="24"/>
        </Grid>
    </materialDesign:Card>

</UserControl>
