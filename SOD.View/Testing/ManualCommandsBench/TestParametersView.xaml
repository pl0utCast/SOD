<UserControl
    x:Class="SOD.View.Testing.ManualCommandsBench.TestParametersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SOD.View.Testing.ManualCommandsBench"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lc="clr-namespace:SOD.LocalizationService;assembly=SOD.LocalizationService"
    xmlns:testing="clr-namespace:SOD.View.Testing"
    xmlns:vm="clr-namespace:SOD.ViewModels.Testing.ManualCommandsBench;assembly=SOD.ViewModels"
    d:DesignHeight="800"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TabControl HorizontalAlignment="Stretch" Style="{StaticResource MaterialDesignTabControl}">
            <TabItem Header="{lc:Localization Testing.ManualCommandsBench.ChoiceOfTestMethod}">
                <local:SelectProgrammMethodicsView Margin="0,10,0,0" DataContext="{Binding ProgrammMethodics}" />
            </TabItem>
            <TabItem Header="{lc:Localization Testing.ManualCommandsBench.Parameters}">
                <local:SelectTestDataView DataContext="{Binding TestData}" />
            </TabItem>
        </TabControl>
        <StackPanel
            Grid.Row="1"
            Margin="5"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button
                Height="50"
                Command="{Binding Cancel}"
                Content="{lc:Localization Testing.ManualCommandsBench.Cancel}"
                FontSize="24"
                Style="{StaticResource MaterialDesignFlatButton}" />
            <Button
                Height="50"
                Command="{Binding Finish}"
                Content="{lc:Localization Testing.ManualCommandsBench.Apply}"
                FontSize="24" />
        </StackPanel>
    </Grid>
</UserControl>
